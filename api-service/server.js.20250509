// api-service/server.js

const express = require('express');
const app = express();
const port = 3000;

// Simple endpoint
app.get('/', (req, res) => {
  res.send('¡Hola, Mundo! API REST en Node.js');
});

// Inicia el servidor
app.listen(3000, '0.0.0.0', () => {
  console.log("Servidor corriendo en http://0.0.0.0:3000");
});
